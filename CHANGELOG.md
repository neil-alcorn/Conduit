# Changelog — conduit

Format: `## [date] — summary`. Updated by the Conduit agent at each gate.

---

## [2026-06-18] Personal TypeScript baseline

Re-based onto the TypeScript CLI; removed org-specific (work-tracker / registry /
portfolio) integrations so Conduit runs as a generic, org-agnostic delivery
orchestration tool on personal projects or any organization.
